"""This directory contains unit tests written for the backend API"""

"""
To execute the tests, run the following commands from the root of the project:

```bash
# Start the development server:
pyhon -m pip install requirements.txt
fastapi --host 0.0.0.0 --port 1140 dev ./api/app.py

# or, if you have pnpm installed:
pnpm dev:backend

# In a new terminal window:
python -m unittest discover --start-directory ./api/tests
```
"""
